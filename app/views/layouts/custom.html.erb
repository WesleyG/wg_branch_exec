<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, maximum-scale=1, initial-scale=1, user-scalable=0">

  <!-- Always force latest IE rendering engine or request Chrome Frame -->
  <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">

  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:400,600,800">

  <!-- Use title if it's in the page YAML frontmatter -->
  <title><%= full_title(yield(:title)) %></title>

  <%= stylesheet_link_tag 'application' %>

  <%= javascript_include_tag  'application' %>
</head>



<body>
<%= render 'layouts/top_navigation_bs3' %>
<%= render 'layouts/side_navigation' %>

<div class="main-content">
  <div class="container">
    <div class="row">

      <div class="area-top clearfix">
        <div class="pull-left header">
          <!-- WGG commenting out crumbs
          <% if false %>
          <h3 class="title">
            <i class="<%= crumbs[:secondary][:icon] %>"></i>
            <%= crumbs[:secondary][:label] %>
          </h3>
          <% end %>
          WGG -->
          <h5>
            <span>
              Evolve Executive Dashboard
            </span>
          </h5>
        </div>

        <ul class="list-inline pull-right sparkline-box">

          <li class="sparkline-row">
            <h4 class="blue"><span>Appointments</span>
              <!-- WGG bryan's spin # ...<span id="app_num"> 1,547</span></h4> -->
              1,547</h4>
              
            <div class="sparkline big" data-color="blue"><!--<%= random_numbers(12).join(",") %>--></div>
          </li>

          <li class="sparkline-row">
            <h4 class="green"><span>Enrollments</span>3,246</h4>
            <div class="sparkline big" data-color="green"><!--<%= random_numbers(12).join(",") %>--></div>
          </li>

          <li class="sparkline-row">
            <h4 class="red"><span>Commissions</span> $998k</h4>
            <div class="sparkline big"><!--<%= random_numbers(12).join(",") %>--></div>
          </li>

        </ul>
      </div>
    </div>
  </div>
<!-- WGG removing breadcrum nav ...
  <div class="container padded">
    <div class="row">

      <!-- Breadcrumb line -->
<!-- WGG removing breadcrum nav cont ...
      <div id="breadcrumbs">
        <div class="breadcrumb-button blue">
          <span class="breadcrumb-label"><i class="icon-home"></i> Home</span>
          <span class="breadcrumb-arrow"><span></span></span>
        </div>
        <!-- WGG adding ruby comment..
        <% if false %>
        <% if link_level == 2 %>
            <div class="breadcrumb-button">
              <span class="breadcrumb-label">
                <i class="<%= crumbs[:primary][:icon] %>"></i> <%= crumbs[:primary][:label] %>
              </span>
              <span class="breadcrumb-arrow"><span></span></span>
            </div>
        <% end %>


        <div class="breadcrumb-button">
          <span class="breadcrumb-label">
            <i class="<%= crumbs[:secondary][:icon] %>"></i> <%= crumbs[:secondary][:label] %>
          </span>
          <span class="breadcrumb-arrow"><span></span></span>
        </div>
      </div>
    </div>
  </div>

WGG         <% end %> <!-- WGG removing ruby comment -->
  <div class="container">
    <%= yield %>
  </div>
</div>

<!-- WGG adding Bryan's number spin script 

<script>
var change_total_due = '1547';

function spinning(id) { 
    var thisid = '#' + id;
    var $obj = $(thisid);
    $obj.css('opacity', '.3');
    var original = $obj.text();

    var spin = function() {return Math.floor(Math.random() * 10);};

    var spinning = setInterval(function() {
        $obj.text(function() {
            var result = '';
            for (var i = 0; i < original.length; i++) {
                result += spin().toString();}
            return result;});
    }, 50);

    var done = setTimeout(function() {
        clearInterval(spinning);
      //$obj.text('$'+change_total_due.toString().replace(/,/g, "").replace(/\B(?=(\d{3})+(?!\d))/g, ",")).css('opacity', '1');

      $obj.text(change_total_due).css('opacity', '1');
    
}, 2000);
}


$( window ).load(function() {
    spinning('app_num');


});

</script>
WGG end of Bryan's script -->



</body>
</html>



